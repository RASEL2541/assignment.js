function woodCalculator(chir,table,bed){
    var chirCount = chir *1;
    var tableCount = table *3;
    var bedCount = bed *5;
    var totalWood = chirCount + tableCount + bedCount;
  return totalWood;
}
